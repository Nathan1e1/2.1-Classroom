# the order.py contains a constructor to create our order object, 
# takes in string and numerical values from the runner / controller

class Coffee:
   # The __init__ should contain all of the values needed for your order object
   # def __init__(self, coffeeBean, hot, size, price):
      #   self.coffeeBean = coffeeBean
      #   self.hot = hot
      #   self.size = size
      #   self.price = price

# class Espresso(Coffee):

#     def __init__(self, coffeeBean, hot, size, price):
#         super().__init__(True, 2.00)
#         self.coffeeBean = coffeeBean
#         self.size = size

# Coffee0 = Coffee("Beans", True, "M", 2.00,)
# Espresso0 = Espresso("Beans", True, "M", 2.00,)