CREATE TABLE orders
    (
    customer_name VARCHAR(20), 
    drink VARCHAR(20),
    temperature VARCHAR(20), 
    size VARCHAR(20, 
    price VARCHAR(20)
    );

INSERT INTO order (customer_name, drink, temperature, size, price)
    VALUES ();